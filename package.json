{
  "name": "pi-lsp-bridge",
  "version": "0.3.2",
  "description": "Collect background diagnostics for Pi from LSPs and repo-configured linters.",
  "keywords": [
    "pi",
    "pi-extension",
    "lsp",
    "diagnostics",
    "linters"
  ],
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node test/run-tests.mjs"
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*",
    "@mariozechner/pi-tui": "*",
    "@sinclair/typebox": "*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "dependencies": {
    "pino": "^10.3.1"
  }
}
